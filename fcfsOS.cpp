#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin>>n;
    vector<int> at,bt,ct,tat,wt;
    queue<int> q;
    for(int i = 0;i<n;i++){
        int x;
        int y;
        cin>>x>>y;
        at.push_back(x);
        bt.push_back(y);
        ct.push_back(0);
        tat.push_back(0);
        wt.push_back(0);
    }
    time = *min_element(at.begin(),at.end())
    q.push(time);


    
}